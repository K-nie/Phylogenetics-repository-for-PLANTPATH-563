Software,Description,Strengths,Weaknesses,Assumptions,User choices
1.RAxML-NG,A from-scratch re-implementation of the RAxML/ExaML greedy tree search algorithm for maximum likelihood (ML) phylogenetic inference.+1,"Finds higher-scoring trees than competitors on taxon-rich datasets and offers 10–60% runtime improvements via ""site repeats"". It is highly scalable, supporting MPI and pthreads for supercomputing.+4",Shows higher variance and lower stability in results compared to tools like IQ-TREE. It may require more replicate searches to find the best tree on alignments with strong signals.+2,"Assumes the popular Maximum Likelihood (ML) criterion for finding optimal trees, which is known to be NP-hard. It also assumes specific substitution models (e.g., GTR) describe the evolutionary process.+1","Users can choose from 22 GTR-derived DNA models , optimize or fix branch lengths , and select support metrics like the Transfer Bootstrap Expectation (TBE). They can also provide starting trees or use parsimony-based ones.+3"